interface:
  display_name: "Video Director"
  short_description: "Create cinematic ad treatments and AI-video prompt workbooks."
  default_prompt: "Use $video-director to develop a product video from concept direction through Phase 2 AI-video prompt spreadsheet."
policy:
  allow_implicit_invocation: true
